# AdaDo

Self-hosted AI assistant suite by Diginoz.
Say it in plain English — AdaDo handles the rest.

## Structure
- `harness/` — core platform (CasaOS fork)
- `apps/` — app store manifests (one YAML per app)  
- `agents/` — agent definitions (one per app)
- `docs/` — documentation
